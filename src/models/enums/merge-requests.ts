export enum MergeRequestState {
  opened = 'OPENED',
  merged = 'MERGED',
  closed = 'CLOSED'
}

export enum MergeRequestType {
  all = 'all',
  closed = 'closed',
  opened = 'opened',
  merged = 'merged'
}
