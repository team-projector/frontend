export enum BreakState {
  created = 'CREATED',
  decline = 'DECLINED',
  approved = 'APPROVED'
}

export enum BreaksType {
  all = 'all',
  created = 'CREATED',
  decline = 'DECLINED',
  approved = 'APPROVED'
}

export enum BreakReasons {
  dayoff = 'DAYOFF',
  vacation = 'VACATION',
  disease = 'DISEASE'
}
